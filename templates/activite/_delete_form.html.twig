{#<form method="post" action="{{ path('activite_delete', {'id': a.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">#}
{#    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activite.id) }}">#}
{#    <button class="btn">Delete</button>#}


{#</form>#}
<form method="post" action="{{ path('deleteActiv', {'id': a.id}) }}" onsubmit="return confirm('Etes vous sur de supprimer cette activité ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ a.id) }}">
    <button  class="btn btn-danger"><i class="fa fa-trash"></i></button>
</form>