{% extends 'admin/bars.html.twig' %}

{% block contenpage %}
    <div class="">
        <div class="page-title">
            <div class="title_left">

            </div>

        </div>

        <div class="clearfix"></div>

        <div class="row">
            <div class="col-md-12 col-sm-12 col-xs-12">
                <div class="x_panel">
                    <div class="x_title">

                        <ul class="nav navbar-right panel_toolbox">
                            <li><a class="collapse-link"><i class="fa fa-chevron-up"></i></a>
                            </li>
                            <li><a class="close-link"><i class="fa fa-close"></i></a>
                            </li>
                        </ul>
                        <div class="clearfix"></div>
                    </div>
                    <h1>hi</h1>
{#                    {% for a in ass %}#}
{#                    {% if a.userA is same as app.user %}#}
{#                    <h2>{{ a.titre }}</h2>#}
{#                    {% endif %}#}
{#                    {% endfor %}#}
{#                    {% for a in activites %}#}

{#                    {{a.id }}#}
{#                        jjj#}

{#                    {% endfor %}#}
{#                    <div class="x_content">#}
{#                        {{ form_start(form1,{'attr':{'enctype':'multipart/form-data'}}) }}#}
{#                        {% if app.user is same as ass.userA %}#}
{#                        {{ ass.titre }}#}
{#                        {% endif %}#}


{#                        {{ form_start(activiteform) }}#}

{#                        {{ form_widget(activiteform) }}#}

{#                        <h5 class="font-weight-bold text-muted">Choisir quelque photos<br/>#}
{#                            <br/>#}
{#                            <label for="upload" class="btn btn-light  rounded-pill " style="width:800px;height:40px">#}

{#                                <input type="file" name="files[]" id="files" multiple="multiple" required/>#}
{#                            </label>#}
{#                        </h5>#}

{#                        <div class="ln_solid"></div>#}
{#                        <div class="item form-group">#}
{#                            <div class="col-md-6 col-sm-6 col-xs-12 col-md-offset-3">#}
{#                                <a class="btn btn-warning" href="{{ path('activite_index') }}">Annuler</a>#}
{#                                <button type="submit" name="valider" class="btn btn-success">Enregistrer</button>#}
{#                            </div>#}
{#                        </div>#}
{#                        {{ form_end(activiteform) }}#}

                        {{ form_start(activiteform) }}
                                            {{ form_widget(activiteform) }}


                        <div class="ln_solid"></div>
                        <div class="item form-group">
                            <div class="col-md-6 col-sm-6 col-xs-12 col-md-offset-3">
                                <a class="btn btn-warning" href="{{ path('activite_index') }}">Annuler</a>
                                <button type="submit" name="valider" class="btn btn-success">Enregistrer</button>
                            </div>
                        </div>
                        {{ form_end(activiteform) }}

                    </div>
                </div>
            </div>
        </div>
    </div>


{% endblock %}
